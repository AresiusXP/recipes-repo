# Release workflow for the Go backend.
# Triggered on tags matching backend-v* (e.g. backend-v1.2.3).

name: Release — Backend

on:
  push:
    tags:
      - "backend-v*"

env:
  REGISTRY: ghcr.io
  CHART_PATH: helm/recipes-backend

permissions:
  contents: write
  packages: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Extract version from tag
        id: version
        run: |
          VERSION="${GITHUB_REF_NAME#backend-v}"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Lowercase registry paths
        id: registry
        run: |
          echo "owner=${GITHUB_REPOSITORY_OWNER,,}" >> "$GITHUB_OUTPUT"
          echo "image=ghcr.io/${GITHUB_REPOSITORY_OWNER,,}/recipes-backend" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push backend image
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          push: true
          tags: ${{ steps.registry.outputs.image }}:${{ steps.version.outputs.version }}
          cache-from: type=registry,ref=${{ steps.registry.outputs.image }}:cache
          cache-to: type=registry,ref=${{ steps.registry.outputs.image }}:cache,mode=max

      - name: Install Helm
        uses: azure/setup-helm@v4

      - name: Update Chart.yaml versions
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          yq -i ".version = \"${VERSION}\"" "${{ env.CHART_PATH }}/Chart.yaml"
          yq -i ".appVersion = \"${VERSION}\"" "${{ env.CHART_PATH }}/Chart.yaml"

      - name: Package and push Helm chart
        run: |
          helm package "${{ env.CHART_PATH }}"
          echo "${{ secrets.GITHUB_TOKEN }}" | helm registry login "${{ env.REGISTRY }}" \
            --username "${{ github.actor }}" --password-stdin
          helm push recipes-backend-${{ steps.version.outputs.version }}.tgz \
            oci://${{ env.REGISTRY }}/${{ steps.registry.outputs.owner }}/charts

      - name: Commit Chart.yaml and CHANGELOG.md to main
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git fetch origin main
          git checkout -f main
          VERSION="${{ steps.version.outputs.version }}"
          yq -i ".version = \"${VERSION}\"" "${{ env.CHART_PATH }}/Chart.yaml"
          yq -i ".appVersion = \"${VERSION}\"" "${{ env.CHART_PATH }}/Chart.yaml"
          node scripts/update-changelog.mjs "backend-v${VERSION}"
          git add "${{ env.CHART_PATH }}/Chart.yaml" CHANGELOG.md
          git diff --cached --quiet && echo "No changes to commit" && exit 0
          git commit -m "chore: bump backend chart and update changelog for v${VERSION} [skip ci]"
          git pull --rebase origin main
          git push origin main
