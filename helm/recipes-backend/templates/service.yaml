apiVersion: v1
kind: Service
metadata:
  name: {{ include "recipes-backend.fullname" . }}
  labels:
    {{- include "recipes-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "recipes-backend.selectorLabels" . | nindent 4 }}
