apiVersion: v1
kind: Service
metadata:
  name: {{ include "recipes-repo.fullname" . }}
  labels:
    {{- include "recipes-repo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "recipes-repo.selectorLabels" . | nindent 4 }}
