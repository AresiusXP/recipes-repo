apiVersion: v1
kind: Service
metadata:
  name: {{ include "recipes-scraper.fullname" . }}
  labels:
    {{- include "recipes-scraper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "recipes-scraper.selectorLabels" . | nindent 4 }}
