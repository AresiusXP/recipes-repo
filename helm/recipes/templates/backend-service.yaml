apiVersion: v1
kind: Service
metadata:
  name: {{ include "recipes.componentFullname" (dict "component" "backend" "context" .) }}
  labels:
    {{- include "recipes.componentLabels" (dict "component" "backend" "context" .) | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "recipes.componentSelectorLabels" (dict "component" "backend" "context" .) | nindent 4 }}
